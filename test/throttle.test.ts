import { describe, it, expect } from 'vitest'
import { throttle, debounce } from '../src/throttle'

describe('throttle', () => {
  it('debounce', () => {
    expect(1).toBe(1)
  })
})

describe('debounce', () => {
  it('', () => {

  })
})
